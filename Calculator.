* {
    box-sizing: border-box; /* the width and height includes the padding properties */
    margin: 0;
  }
  
  .calculator {
    background-color: blue;
    padding: 20px;
    max-width: 400px;
    margin: 0 auto;
    border: solid 1px #ccc;  /* gives a border around the calculator*/
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);  /* used for 3D effect */
    border-radius: 10px; /* corners are rounded */
    margin-top: 40px;
  }
  
  #result{
     background-color: aqua;
      width: 100%;
      padding: 10px;
      font-size: 24px;
      border: none;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3) inset;
      border-radius: 5px;
  }
  
  .buttons{
      display: grid; /* used for setting grid layout*/
      grid-template-columns: repeat(4, 1fr); /* sets 4 equal-width coloumns */
      grid-gap: 10px;
      margin-top: 20px;
  }
  
  button{
      padding: 10px;
      font-size: 24px;
      border: none;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
      border-radius: 5px;
      cursor: pointer; /* changes the cursor to a pointer while hovering over it */
      transition: background-color 0.3s ease; /* when hovering on a button, the backgound color smoothly changes */
  
  }
  
  button:hover{
      background-color: #ddd;
  }
  
  .clear{
      background-color: #ff4136;
      color: #fff;
  }
  
  .number, .decimal{
      background-color: #fff;
      color: #333;
  
  }
  
  .operator{
      background-color: red;
      color: #fff;
  }
  
  .equals{
      background-color: palevioletred;
      grid-row: span 3; /* combining 3 rows to make it taller */
      color: #fff;
  }
